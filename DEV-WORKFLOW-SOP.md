# 日常开发会话 SOP（主 Agent 统筹模式）

> 适用项目：windrider / multica。目标是：**状态进仓库、会话是工作台、一个任务一个会话、主 Agent 统筹子 Agent 干活**。

---

## 一、核心原则

1. **状态进仓库，不进会话**：会话会过期、会压缩、会清空；仓库里的 `AGENTS.md` 永远在。每次会话结束，更新 AGENTS.md 的「当前任务状态」3 行。
2. **一个会话 = 一个自然任务（或最多一天）**。别让会话长成 28 天 200 次压缩的怪物。
3. **主 Agent 统筹，子 Agent 干活**：你只跟主 Agent 说话；主 Agent 拆任务、派子 Agent、审查、集成、提交。

## 二、会话纪律（最重要）

| 动作 | 触发点 | 怎么做 |
|---|---|---|
| **开局** | 每次新会话 | 第一句：`读 AGENTS.md，按当前状态继续 <任务>` |
| **收尾** | 任务完成 / 上下文 ~7-8 万 / 一天结束 | 更新 AGENTS.md 3 行交接 → 关会话 |
| **禁止** | — | 一个会话里连续跨多个不相关任务；靠"继续"无限续命 |

> 看上下文：桌面端右上角有上下文占用显示（`show-context-window-usage`）。到 ~7-8 万就该收尾，别等到 15 万触发压缩。

## 三、主 Agent 统筹 · 每日循环

```
① 开局: 读 AGENTS.md，说今天目标
② 拆解: 主 Agent 把目标拆成 2-4 个任务
③ 分派: 每个任务 spawn 一个子 Agent，给它【明确写集】= 只能动哪几个文件
④ 审查: 子 Agent 回来，主 Agent 看 diff、跑测试，不行打回重做
⑤ 双审: 关键任务再派一个独立子 Agent 查「规范符合 + 代码质量」
⑥ 集成: 主 Agent 合代码、跑完整验证、提交
⑦ 收尾: 更新 AGENTS.md 交接 3 行
```

**具体命令/工具**：
- 子 Agent：用 `multi_agent_v1__spawn_agent`（**不要用 `create_thread`**）
- 子 Agent 用 `high` reasoning 就够，**不要给子 Agent 开 ultra**
- 一次只开 1-2 个子 Agent，别一窝蜂

## 四、开局 / 收尾模板

**开局（新会话第一句）**：
```
读 AGENTS.md，按当前任务状态继续。今天做：<一句话目标>。
```

**收尾（AGENTS.md 更新模板）**：
```
- 做了什么: <这轮完成的 1-2 件事>
- 做到哪: <当前进度、在哪个分支/工作树>
- 下一步: <下一会话从哪开始，越具体越好>
```

## 五、项目工作树位置速查

| 项目 | 工作树 | 备注 |
|---|---|---|
| **windrider** | `~/.config/superpowers/worktrees/windrider/<分支>` | openclaw-ui-v2 / openclaw-product-loop 等，稳定 |
| **multica** | `~/.config/superpowers/worktrees/multica/<任务名>` | 开工前确认当前集成分支和任务 worktree；不得把旧会话里的分支保护说明当作永久规则 |

## 六、Token 纪律（做对上面这些，自动省 token）

- 子 Agent 上下文新鲜 → 不累积 → 天然省
- 主 Agent 上下文靠「一个任务一个会话」控制
- exec 大输出：写文件再 tail/grep，别整段回传（RTK.md 已全局生效）
- 会话到 7-8 万果断收尾，别靠压缩续命
- 每次收尾更新 AGENTS.md，就是给下一个会话"免费"的热启动

---

*本 SOP 于 2026-08-06 制定。核心：少开长会话、状态留仓库、主 Agent 统筹、子 Agent 专注。*
