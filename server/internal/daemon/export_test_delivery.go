package daemon

// DesignDeliverySchemaForTest exposes the daemon's copy of the delivery wire
// schema so a cross-boundary test can prove it still matches the server's.
const DesignDeliverySchemaForTest = designDeliverySchema
