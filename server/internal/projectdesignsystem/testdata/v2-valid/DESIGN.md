# Soyoung CRM Design System

The interface favors dense operational scanning, restrained accents, and clear status hierarchy.

## Data Workspaces

Use compact filters, aligned table columns, and persistent primary actions for repeated CRM work.

## Interaction States

Keep hover, selected, disabled, warning, and empty states visually distinct without changing layout density.
